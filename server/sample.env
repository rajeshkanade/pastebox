# Sample .env for pastebox-file-sharing-platform
# Copy this file to .env and fill in real values before running the server.
# WARNING: Do NOT commit your real .env to version control.

# Database
MONGODB_URL=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>

# Server
PORT=6600
SERVER_URL=http://localhost:6600/api/files
CLIENT_URL=http://localhost:5173
NODE_ENV=development
BASE_URL=http://localhost:6600

# Authentication
JWT_SECRET=your_jwt_secret_here

# Supabase (Storage)
SUPABASE_URL=https://your-supabase-project.supabase.co
# Use the SERVICE_ROLE or SERVICE_KEY for server-side operations (keep secret)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key
SUPABASE_BUCKET=pastebox-files

# (Optional) AWS S3 settings - only if you use S3
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=your_aws_region
AWS_BUCKET_NAME=your_bucket_name

# Mail (for sending links)
MAIL_USER=your_email@example.com
MAIL_PASS=your_email_password

# Notes:
# 1. After copying to .env and filling values, restart the Node server.
# 2. Ensure `dotenv.config()` is called once at application start (e.g. in src/app.js or src/index.js).
# 3. For Supabase, create the `SUPABASE_BUCKET` in the Supabase dashboard and set public/policies as required.
